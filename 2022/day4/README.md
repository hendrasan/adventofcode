A naive approach that creates array for each range and then compare whether array A includes every item in array B and vice versa.

Perhaps can use math to determine whether a range is in another range.


